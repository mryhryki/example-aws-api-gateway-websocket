{
  "name": "example-aws-api-gateway-websocket",
  "author": "hyiromori",
  "license": "MIT",
  "private": true,
  "scripts": {
    "deploy": "source .env && serverless deploy",
    "setup": "cp .env.template .env"
  },
  "dependencies": {
    "aws-sdk": "^2.779.0",
    "serverless-pseudo-parameters": "^2.5.0"
  }
}
